## 1. Five-mode runtime and workflow cutover

This delivery unit produces the single mergeable implementation MR. Direct user instruction exempts this bootstrap migration from the future mandatory-POC workflow; local review, CI, and latest-head Nitro review exercise the real implementation directly.

- [x] 1.1 Create the five self-contained mode packages and lifecycle contract.
  - First real confirmation: Run the installed-mode workflow scenario; an unresolved Plan request reports no artifact, an Explore request reports a denied repository write, and a settled atomic Plan request creates only `.agents/plans/five-mode-proof.md` in its owned disposable worktree. Keep the pass/fail output task-local.
  - Acceptance: `skills/{explore,plan,execute,review,finish}` own portable behavior; `session-start` moves into the startup rule and mode preflights; the source wildcard is replaced before modes become selectable; semantic routing has no numeric thresholds; target-specific four-reviewer baselines run read-only; one coordinated writer owns each worktree; exactly one workflow-policy profile resolves Linear/provider defaults; subsequent OpenSpecs use one full draft POC with exact-SHA automated/personal review, one reconciliation batch per authorized cycle, and final MRs shaped by top-level delivery units; this bootstrap exception produces one implementation MR with latest-head Nitro review; Review emits the publication checkpoint; Finish respects provider and terminal-action authority.
  - Verification: Use `writing-skills` RED/GREEN/REFACTOR scenarios, copied-skill execution without AI-repo dependencies, skill/profile validation, ordinary-language and explicit OpenSpec-adapter fixtures, direct escalation, artifact selection, planning review, POC task-state handling, Nitro/personal review and resume, ambiguous policy profiles, approved Linear status transitions, reconciliation without automatic rerun, one-unit and multi-unit final delivery, total Git order versus logical dependencies, squash-restack behavior, hosted-feedback handoff, publication-checkpoint invalidation, retained-specialist cross-link scans, managed/unmanaged collisions, and no-merge defaults.

- [x] 1.2 Build local runtime state and immutable source snapshots.
  - Acceptance: `ax.config.json` remains desired state; `~/.agents/runtime/managed-runtime.json` stores only schema/hash version, installed profiles, one workflow-policy profile, owned paths, and content hashes; `sha256-tree-v1` covers path/kind/executable-bit/file-or-link payload/empty directories; manifest-less occupied paths require exact-hash `manage|replace-managed|remove` approval from an interactive preview or `--adoption-file`; replacement/removal backs up observed legacy content; AX never reads `ax.lock.json`; remote URL/ref pairs resolve latest once; Git-tree or triple-hash local snapshots prevent mixed versions; caches live under `~/.agents/runtime/cache`.
  - Verification: Cover first interactive/headless profile and policy-profile selection, later interactive replacement/addition, hash-bound headless profile-selection file, candidate-matching adoption, drifted desired-path replacement, retired/stale removal, headless adoption file, hash/version drift, files/symlinks/executable bits/empty directories, incidental metadata stability, legacy backups, unmanaged collisions, latest-ref advancement without persisted commits, one snapshot shared by several entries, local source mutation/retry/failure, cache recreation, no-op stability, and manifest schema rejection.

- [x] 1.3 Build recoverable runtime synchronization with seven-backup retention.
  - Acceptance: top-level `ax sync` alone bootstraps/changes profiles; scoped sync requires a valid manifest and changes only its surface; every sync holds one runtime-root mutation lock; domain planners submit operations to a neutral transaction engine; transaction directories store preimages, hash-verified candidate payloads, deletion markers, and phase/manifest/target hashes; recovery finalizes candidates or restores by hashes without rebuilding from disposable source state; mixed recovery choices produce a journaled derived manifest that restores previous ownership, applies candidate ownership, or preserves content without ownership per path and installs atomically last; profile/policy changes require one exact-hash-bound previous/candidate metadata selection and reject out-of-inventory ownership; first-sync previous state is an absent-manifest outcome with zero ownership; external edits and failed restoration preserve blocking recovery state; dead-owner locks are reclaimed safely; unmanaged/system entries remain untouched; seven verified backups remain per asset/target.
  - Verification: Cover scoped-before-init rejection, scoped profile immutability, concurrent sync rejection, dead/live lock owners, failures before mutation and after each surface, process death before/after normal or derived manifest replacement/deletion and phase recording, external edits during recovery, failed rollback, mixed hash-bound `restore-previous|apply-candidate|preserve-unmanaged` resolution across profile/policy changes after cache deletion and local/remote source advancement, first-sync previous-state restoration, incompatible profile/path choices, older or unmanaged desired drift after successful recovery, stale target/manifest hashes, idempotent finalize/restore, owned pruning, collision blocking, backup rotation, unchanged-content no-op behavior, live-source rejection, and isolated-root POC/final-branch execution.

- [x] 1.4 Converge OpenSpec and offline inspection on sync semantics.
  - Acceptance: `ax openspec sync` resolves/reports the PATH OpenSpec CLI without installing it, handles missing/configured/partial state with context authorization, candidate generation, explicit-only adapter normalization, and a worktree-locked repo transaction/backup domain outside the runtime manifest with retained candidate payloads; compare-before-replace/restore preserves concurrent edits; `status`/`validate` remain offline/read-only; runtime/OpenSpec install and update fail with `Use ... sync`; shim lifecycle remains available.
  - Verification: Cover CLI path/version diagnostics and unavailable CLI, interactive/headless missing setup, configured drift, partial state with/without config, context/config-review flags, canonical asset/link targets, unrelated dirty paths, same-worktree contention, two repository identities, concurrent touched-file edits, failed repo rollback, hash-bound recovery choices after source/cache changes, preserved noncanonical drift after successful recovery, stale inputs, repeated normalization, deliberately denormalized adapters, offline local-consistency reporting, removed-command errors, and top-level sync that leaves repo-local OpenSpec unchanged.

- [x] 1.5 Complete the public cutover with active-surface migration.
  - Acceptance: use the documented pre-cutover bootstrap and current `ax commit`; retire overlapping lifecycle packages, `session-start`, and orphaned helpers; remove tracked private `.agents/plans` YAML sidecars, `ax commit`, `ax review-gate`, `ax plans artifact`, tracked `ax.lock.json`, and repo-local cache; make the hook source repository-relative and snapshot-backed; update active instructions, startup rules, hook/AX guidance, provider references, retained specialists, fixtures, and tests; keep live runtime unchanged before merge and document post-merge `ax sync`.
  - Verification: Run `writing-skills`, instruction/rule snapshots, a scan allowing only primary Markdown under `.agents/plans`, forbidden-reference scans excluding archived history, `pnpm biome:lint-format`, `pnpm exec tsc --noEmit`, `pnpm skills:validate`, strict OpenSpec validation, focused AX/mode tests, `pnpm test`, isolated personal/work sync/status/validate, a disposable repository test of OpenSpec task completion/archive, final planning review of the actual canonical-spec/archive changes, and an exact-HEAD target-base diff review with correctness, regression, maintainability, verification, documentation-alignment, and AX-compatibility reviewers.
