interface:
  display_name: "Finish"
  short_description: "Publish safely without implicit merge"
  default_prompt: "Use $finish to resolve provider policy, publish a hook-clean draft, request hosted review, start local Review on the same head, follow hosted feedback, report technical readiness, and merge, deploy, or clean up only with explicit terminal authority."
