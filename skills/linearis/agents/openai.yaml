interface:
  display_name: "Linearis"
  short_description: "Operate Linear through its JSON CLI"
  default_prompt: "Use $linearis to perform this Linear provider operation through the linearis CLI within the active semantic and lifecycle authority."
