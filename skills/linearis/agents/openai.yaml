interface:
  display_name: "Linearis"
  short_description: "Use the fallback Linear JSON CLI safely"
  default_prompt: "Use $linearis as the fallback CLI adapter for this Linear provider operation within the active semantic and lifecycle authority."
