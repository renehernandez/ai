interface:
  display_name: "Execute"
  short_description: "Implement with one owned writer"
  default_prompt: "Use $execute to verify one-writer worktree ownership and environment preflight, implement direct work, a complete OpenSpec POC, or one final delivery unit with progressive verification, batch Review findings after phase barriers, run the complete stable-head Review wave, and stop before unauthorized terminal actions."
