interface:
  display_name: "AX CLI"
  short_description: "Synchronize and inspect AX runtime assets"
  default_prompt: "Use $ax-cli to inspect AX-managed state, initialize or switch one machine profile with top-level ax sync --profile, use scoped sync without changing that selection, preserve tracked profile definitions and exact target ownership, and validate the affected surface. Keep feature-branch proof on isolated runtime roots, register saved coordinator project IDs only after live post-merge sync, and activate live roots only from verified merged source."
