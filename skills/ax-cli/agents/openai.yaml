interface:
  display_name: "AX CLI"
  short_description: "Synchronize and inspect AX runtime assets"
  default_prompt: "Use $ax-cli to inspect AX-managed state, initialize or switch one machine profile with top-level ax sync --profile, use scoped sync without changing that selection, preserve tracked profile definitions and local selection state, and validate the affected surface. Keep feature-branch proof on isolated runtime roots and activate live roots only from verified merged source."
