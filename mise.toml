[tools]
node = "26.3.0"
"npm:lefthook" = "2.1.6"

[tasks.setup]
description = "Install dependencies and configure git hooks"
run = [
  "corepack enable",
  "pnpm install --frozen-lockfile",
  "lefthook install",
]

[tasks.pre-commit]
description = "Run Lefthook pre-commit commands"
run = "lefthook run pre-commit"
